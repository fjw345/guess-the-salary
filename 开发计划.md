# 猜薪资 开发计划

给出「学历 / 学校 / 专业 / 毕业已经 / 城市 / 公司 / 岗位」七个维度，玩家猜薪资，提交后揭晓真实薪资与投稿者留言，接近程度由玩家自行判断。无限随机出题，前后端分离，数据以社会投稿（UGC）为主、种子数据启动。

**维度以实际收集到的数据为准。** 种子数据（[种子数据.xlsx](种子数据.xlsx)，123 条）实际有效列为：学历、学校、专业、毕业已经、城市、公司名称、岗位、薪资、想说的话。行业列收集不到，**从数据模型里移除**——空字段既没有出题价值，也会误导后续投稿表单设计。行业信息在多数情况下可由公司名和岗位推断，玩家不需要它单列。

**当前版本不做自动评分。** 种子数据里税前/税后、年薪/月薪的口径混杂，在口径统一之前，任何分数都是拿不准的数算出来的假精确。先做「猜 → 揭晓 → 对比」的闭环，评分留到数据口径干净之后。


## 1. 动工前要定的四件事

### 1.1 毕业已经：原样保留文本，另存一个可选的数值

原计划想加 `yearsOfExperience`（数值）。实际数据里这一列是「2年」「刚毕业」「三个月」「一年半」「6个月」「未知」混排——既有整年、有月、有中文数字，也有无法量化的状态。

强行解析成数字会丢信息：「刚毕业」不等于 0 年，它同时说明这个人还没有毕业后的工作时长，这对猜薪资是有用的线索。方案是两个字段并存：

```
tenureText     String        原样保留，如「刚毕业」「一年半」，出题时直接展示这个
tenureMonths   Int?          能解析出来才填，仅用于抽样和统计，解析不出留空
```

出题展示 `tenureText`，玩家看到的就是投稿者自己的说法。`tenureMonths` 只在内部用，不参与展示，也不要求覆盖全部数据。

### 1.2 记录薪资口径，而不是强行统一


原计划要把所有数据折算成「税前年度总包」。种子数据做不到这件事——实际的薪资列长这样：

```
税前月1w          税前年60w        税前月薪7k，年终奖8w
实习期税前月薪8000  月薪税前7500元    税前37万元/年
税后74万元/年      80w             120w+          4.4W/月
```

税前税后、年薪月薪、单位（w / 万 / k / 元）、是否含年终、是否实习期全都混在一句话里，还有「120w+」这种带上界符号的。硬折算等于凭猜测补齐缺失前提。

改为**原文 + 解析结果并存**：

```
salaryRaw      String        投稿原文，如「税前月薪7k，年终奖8w」，永久保留
salaryAmount   Int?          解析出的主数值（元）
salaryPeriod   MONTHLY | ANNUAL | UNKNOWN
salaryBasis    PRETAX | AFTERTAX | UNKNOWN
salaryIsIntern Boolean       是否实习期薪资，实际数据里有 3 条
salaryHasPlus  Boolean       原文带「+」等上界模糊标记
```

`salaryRaw` 是这个设计的关键：**揭晓时展示原文，不展示解析结果**。原文「税前月薪7k，年终奖8w」比任何结构化字段都准确，解析只用于抽样分桶和异常检测。解析失败不影响出题。

出题时把口径（period / basis / isIntern）告诉玩家，让玩家按同一口径猜；金额藏到作答之后。

等投稿数据积累到足够、口径能在表单层面约束住，再回头做统一折算和评分。

### 1.3 学校用标签表达层次，不用单一枚举

原计划的 `schoolTier` 是单选枚举（C9 / 985 / 211 / …），这套东西表达不了真实情况：华中科技大学同时是 985、211、双一流；北京交通大学是 211 和双一流但不是 985；海外院校根本不在这个体系里，只能用 QS 排名。单选逼你在「985」和「双一流」之间挑一个，信息就丢了。

改为**多标签**：

```prisma
model School {
  id     Int      @id @default(autoincrement())
  name   String   @unique      // 华中科技大学
  tags   String[]              // ["985", "211", "双一流"]
}
```

标签取值（一所学校可带多个）：

| 类别 | 标签 |
|---|---|
| 国内层次 | `C9` `985` `211` `双一流` `一本` `二本` `专科` |
| 海外排名 | `QS前10` `QS前50` `QS前100` `QS前200` `QS200+` |
| 特色 | `强基` `两电一邮` `五院四系` `建筑老八校` `医学强校`（选做） |

对照实例：

- 华中科技大学 → `985` `211` `双一流`
- 北京交通大学 → `211` `双一流`
- 南京邮电大学 → `双一流`
- 广东工业大学 → `一本`
- 石家庄学院 → `二本`
- 北京青年政治学院 → `专科`
- 哈佛大学 → `QS前10`
- 南洋理工大学 → `QS前100`

三点实现约束：

**标签是派生数据，不由投稿者填。** 投稿者只填校名，标签由 `School` 表查出来。让投稿者自选标签会得到大量「我们学校算双一流吧」的错误标注。

**校名要做归一化。** 「华科」「华中科大」「华中科技大学」是同一所。`School` 表加一个 `aliases String[]`，清洗时按别名匹配。种子数据里已出现的 6 所（北京交通大学、北京科技大学、清华大学、北京大学等）先手工录入，之后按投稿增量补。

**匹配不上的学校不阻塞出题。** 新学校入库时 `tags` 为空数组，出题时该行不显示标签。空标签比错标签好。

展示上，`985 211 双一流` 三个标签并排会很啰嗦。建议出题只显示最高层级的 1-2 个（按 `C9 > 985 > 211 > 双一流 > 一本 > 二本 > 专科` 优先级取首个，海外取 QS 标签），完整标签留给筛选和统计用。

### 1.4 隐私与线索展示底线

学校和公司是本产品的核心推理线索，默认展示投稿中的具体名称，不做按样本数量触发的整体自动脱敏。隐私保护采用审核时的单项控制：

- 不收集姓名、手机号、邮箱、工号等任何身份信息
- 学校名称和公司名称默认可见；管理员发现某一项存在定位风险时，可分别勾选「隐藏学校名称」或「隐藏公司名称」
- 隐藏学校后仍保留学校层级标签；隐藏公司后显示「公司信息已隐藏」，其他线索不受影响
- 投稿页显式声明「只投自己的或已公开的信息」，并提供举报入口

投稿者仍可主动使用「某大厂」「某设计院」这类自然模糊的公司写法；系统不会把具体学校或公司自动替换成标签。


## 2. 技术栈

| 层 | 选型 | 理由 |
|---|---|---|
| 前端 | React + Vite + TypeScript + Tailwind | 轻，静态产物易部署 |
| 状态 | Zustand | 一局游戏状态量很小，不需要 Redux |
| 后端 | Node + TypeScript + Fastify | 比 Nest 轻，限流 / CORS / 校验插件齐全 |
| ORM / DB | Prisma + PostgreSQL | 题库是关系型数据，迁移可控 |
| 校验 | Zod（前后端共用） | 一份 schema 两端复用 |
| 测试 | Vitest + Playwright | 工具函数单测 + 一局 E2E |
| 部署 | 国内轻量服务器 + Docker Compose + Caddy，域名需 ICP 备案 |

仓库用 npm workspaces 单仓三包：

```
packages/shared/   Zod schema、枚举字典、薪资口径格式化（前后端共用）
apps/web/          React 前端
apps/api/          Fastify 后端 + Prisma
```

`shared` 现在承载的是薪资口径的格式化与展示逻辑，以及学校标签的优先级排序。前后端用同一份，题面和投稿预览才不会出现两种说法。评分函数暂不实现。


## 3. 数据模型

按 30 条种子数据的实际列重写。原模型的 `industry`、`companyTier`、`schoolTier`、`yearsExp`、`level`、`salaryScope` 六个字段全部移除——前五个收集不到，最后一个在真实数据里无法可靠区分。

```prisma
model Submission {
  id             String   @id @default(cuid())

  // === 题面维度（七个，与 xlsx 列一一对应）===
  degree         Degree            // 专科/本科/硕士/博士
  schoolId       Int?              // 关联 School，匹配不上留空
  schoolNameRaw  String            // 投稿原文校名，归一化失败时仍可展示
  major          String            // 「铁道运输」「计算机技术」等，原文保留
  tenureText     String            // 「2年」「刚毕业」「一年半」「未知」原样
  tenureMonths   Int?              // 解析成功才填，仅内部使用
  city           String            // 含「新加坡」「广西某城市」这类非标准值
  companyName    String            // 「某大厂」「未知」，允许模糊
  position       String            // 「区段站」「码农」「算法工程师」原文保留

  // === 答案 ===
  salaryRaw      String            // 投稿原文，揭晓时展示这个
  salaryAmount   Int?              // 解析出的数值（元），失败留空
  salaryPeriod   SalaryPeriod      // MONTHLY / ANNUAL / UNKNOWN
  salaryBasis    SalaryBasis       // PRETAX / AFTERTAX / UNKNOWN
  salaryIsIntern Boolean  @default(false)
  salaryHasPlus  Boolean  @default(false)
  roughAnnual    Int?              // 粗算年度值，仅供抽样分桶
  authorNote     String?           // 「想说的话」，揭晓时展示

  // === 元数据 ===
  status         ReviewStatus @default(PENDING)
  rejectReason   String?
  sourceType     SourceType        // SEED/SURVEY/SELF_REPORT
  submittedAt    DateTime @default(now())
  reviewedAt     DateTime?
  ipHash         String?           // 种子数据无此项，故可空

  school         School?  @relation(fields: [schoolId], references: [id])

  @@index([status, roughAnnual])
}

model School {
  id          Int      @id @default(autoincrement())
  name        String   @unique      // 规范名：华中科技大学
  aliases     String[]              // ["华科", "华中科大"]
  tags        String[]              // ["985", "211", "双一流"]
  submissions Submission[]
}

model GameRound {
  id            String   @id @default(cuid())
  sessionId     String
  submissionId  String
  guessAmount   Int?              // 玩家猜测（元），按题面口径
  guessPeriod   SalaryPeriod?     // 玩家猜的是月薪还是年薪
  servedAt      DateTime @default(now())
  answeredAt    DateTime?

  @@index([sessionId])
}
```

四处设计说明：

**`major` / `position` / `city` 用自由文本，不用枚举。** 这跟之前的计划相反，改的理由是实际数据不配合：岗位列有「区段站」「烟草局」「电网变电所员工」「体制内」「画室老师」，这些既不是标准岗位名，也无法归入任何预设字典——但它们恰恰是题目最有意思的部分。城市有「广西某城市」「新加坡」。强行套枚举只会把这些数据挤成「其他」，题目就变味了。代价是统计维度受限，等样本量大了再做归一化映射表。

**`salaryRaw` 是答案本体，`salaryAmount` 是辅助。** 揭晓时展示原文。解析用于抽样和异常检测，解析不出来不影响这条数据可用。

**`GameRound` 多了 `guessPeriod`。** 题面会告诉玩家猜月薪还是年薪，但记录下玩家实际按什么口径提交，将来做统计时能发现「题面说月薪但玩家按年薪猜」这类交互问题。

**`schoolNameRaw` 和 `schoolId` 并存。** 归一化失败时仍能展示原文校名，只是没有标签。避免因为学校表不全而丢数据。

`roughAnnual` 的粗算规则：月薪 ×12、税后不上调、「120w+」按 120w 计。它不准，但**只用于分层抽样**（避免连续出高薪题），永不展示、永不参与判定。抽样不需要它准。

`GameRound` 里没有 `score` 字段。保留 `guessAmount` 是有价值的——将来做统计（玩家平均猜偏多少、哪类题最容易被低估）靠的是它，评分函数补上后也能回溯计算历史局次。



## 4. 揭晓设计（替代评分）

不打分，但也不能只甩一个数字了事——玩家需要知道自己猜得怎么样，这个反馈由**客观事实**提供，由玩家自己下结论。

揭晓页展示四样东西：

**1. 薪资原文**
> **税前月薪 7k，年终奖 8w**

直接展示 `salaryRaw`，不做改写。原文自带的限定条件（「年终奖8w」「实习期」）比任何结构化渲染都准确。

**2. 玩家的猜测，并列摆放**
> 你猜：月薪 12000 元

**3. 中性的差异陈述**
> 比原文的月薪部分高 5000 元（约 71%）

只陈述事实，不加「很接近」「差得远」这类评判——那正是让玩家自己判断的部分。原文含多个数字时（如月薪 + 年终奖），明确说明在跟哪一部分比。解析不出金额的题目跳过这一段，只并列展示两个数。

**4. 投稿者的话**
> 「尽可能去国企吧，稳定些，至少考进来了不担心他莫名其妙把你裁了」

这是种子数据里最有价值的一列，30 条留言几乎都言之有物。它给了玩家继续玩下一题的理由——分数留不住人，别人的真实处境能。这一段应该在视觉上占足够权重，不要做成灰色小字。

有两个可选项建议先不做：一是玩家自评（「你觉得自己猜得准吗」按钮），多一次点击换来的数据价值不大；二是同维度其他样本的分布，样本量到 200 以上再说，现在展示只会暴露数据稀疏。

## 5. 分阶段实施

### 阶段 0 · 脚手架（0.5 天）
npm workspaces 三包初始化、Prisma 接本地 Postgres（Docker）、`/health` 打通、前端 `/api` 代理、ESLint + Prettier + Vitest 跑通。

> 实施记录：原计划用 pnpm，但在本机 Windows 环境下 `pnpm install` 安装 esbuild 时稳定报 `ERR_PNPM_EPERM`（临时目录改名失败），排查未定位到根因（已排除杀软拦截与文件锁），改用 npm workspaces 后一次装成。包内依赖用 `"*"` 而非 pnpm 的 `workspace:*`。

### 阶段 1 · 数据导入（2 天，比原计划多 0.5 天）
写 xlsx 导入脚本 `scripts/import-seed.ts`，把 [种子数据.xlsx](种子数据.xlsx) 的 30 条读进库。四个子任务：

- **薪资解析器** `parseSalary(raw)`：处理 `税前月1w` / `税前年60w` / `月薪税前7500元` / `税后74万元/年` / `4.4W/月` / `120w+` / `税前月薪7k，年终奖8w` / `实习期税前月薪8000`。识别单位（w/万/k/元）、周期、税前后、实习期、`+` 标记。解析不出的字段留空，**不猜**。
- **毕业时长解析** `parseTenure(text)`：`2年`→24、`三个月`→3、`一年半`→18、`6个月`→6；`刚毕业`、`未知`→ null。`tenureText` 始终原样保留。
- **学校表初始化**：种子数据涉及 4 所（北京交通大学 `211` `双一流`、北京科技大学 `211` `双一流`、清华大学 `C9` `985` `211` `双一流`、北京大学 `C9` `985` `211` `双一流`），手工录入并带别名。
- **导入报告**：打印每条的解析结果，标出解析失败的字段。人工过一遍再入库。

枚举只保留 `degree`、`salaryPeriod`、`salaryBasis`、`status`、`sourceType` 五个。城市/岗位/专业**不做枚举**，理由见数据模型一节。

### 阶段 2 · 展示格式化 + 单测（1 天）
写 `shared/salary.ts` 和 `shared/school.ts`：
- `parseSalary(raw)` — 与导入脚本共用同一份，这是它放在 shared 的原因
- `describeGuessDiff(guess, parsed)` — 返回方向与百分比，无法比较时返回 null
- `primarySchoolTags(tags)` — 按 `C9 > 985 > 211 > 双一流 > 一本 > 二本 > 专科` 取最高 1-2 个，海外走 QS 分支

单测拿真实的 30 条薪资原文当测试用例，逐条断言解析结果。这比自己编造用例有效——真实数据的脏法是编不出来的。另外覆盖：`UNKNOWN` 周期时不渲染成空白、`120w+` 保留上界标记、除零保护、空标签数组返回空。

### 阶段 3 · 出题与作答接口（1.5 天）
- `GET /api/rounds/next?sessionId=` — 从 APPROVED 里分层抽样（按 `roughAnnual` 分桶，避免连续出高薪题），返回七个维度 + 学校标签 + **薪资口径**（period / basis / isIntern），但**不含金额、不含原文、不含留言**，落一条 GameRound
- `POST /api/rounds/:id/answer` — 收 guess，写回，返回 `{ guess, salaryRaw, parsed, diff, authorNote }`；已作答的 round 拒绝重复提交
- `GET /api/stats/:sessionId` — 累计局数、平均偏差百分比（不是分数）

`salaryRaw` 和 `authorNote` 都必须留在作答之后返回——留言里经常直接提到薪资数字（「实习期税前4800，转正税后平均7、8000」），提前下发等于泄题。

口径要在**出题时**就下发，玩家得知道自己在猜月薪还是年薪、税前还是税后。这一条比原方案更要紧：口径没说清就让人猜，猜错的是题目而不是玩家。`salaryPeriod` 为 `UNKNOWN` 的题目（如原文只写「80w」）默认按年薪提示，并标注「口径不明」。

### 阶段 4 · 游戏主界面（2 天）
卡片展示七个维度、学校名旁挂标签徽章、题面醒目标出「请猜税前月薪」、滑块 + 数字输入（滑块用对数刻度，且**量程随 period 切换**——猜月薪时 3000-100000，猜年薪时 5 万-300 万，共用一套量程会让月薪题没法精调）、提交后揭晓页按上一节四段式展示、「下一题」循环。键盘可操作、滑块带 `aria-valuetext`、差异用文字而非仅靠颜色表达，无障碍不要事后补。

顶部不放累计分数条，改放「已猜 N 题」和「平均偏差 X%」。

### 阶段 5 · 投稿板块（2 天）
表单字段与 xlsx 列对齐，共 9 项：学历（选）、学校（填 + 自动补全）、专业（填）、毕业已经（填）、城市（填）、公司名称（填）、岗位（填）、薪资（**先选月薪/年薪 + 税前/税后，再填金额**）、想说的话（填）。

- 薪资改为结构化输入，不再让投稿者写自由文本。种子数据的口径混乱正是自由文本导致的，新投稿从源头上避免，`salaryRaw` 由结构化字段拼回原文
- 学校字段做自动补全，命中 `School` 表则显示标签让投稿者确认，未命中允许自由填写
- 提交前脱敏提示与勾选确认，明确「公司名可以写『某大厂』」
- 反滥用：`ipHash` 限流、验证码、异常值拦截（月薪 < 1000 或 > 30 万、年薪 < 1.2 万或 > 500 万打回）
- 投稿成功页说明「审核后进入题库」

### 阶段 6 · 审核后台（1.5 天）
UGC 项目里没有审核就是没有题库质量。极简实现：单一管理员密码（bcrypt 存 hash，别硬编码）、待审列表、通过 / 驳回（带理由）、批量操作、重复投稿检测。三个针对本项目的必备功能：

- **修正薪资口径**：投稿者选错「税前/税后」比填错金额更常见
- **绑定学校**：把未匹配的校名归一化到 `School` 表，或新建条目并打标签
- **编辑留言**：留言里可能带身份信息或直接泄露薪资，需要能删改

### 阶段 7 · 打磨与上线（1.5 天）
Playwright 跑一局完整流程 + 一次投稿流程、错误边界与空题库兜底、移动端适配、结果分享卡片、隐私说明页、生产部署与备份。

合计约 12 天单人工作量。阶段 0-4 是可玩的最小闭环（约 6.5 天），30 条种子数据足够跑通并验证手感，之后再接投稿。

## 6. 首个里程碑的验收标准

- `npm run test` 全绿，`parseSalary` 对 30 条真实原文的解析结果逐条符合预期
- 30 条种子数据全部成功入库，解析失败的字段为空而非错值
- 前端调 `next` → 题面写明要猜的口径、学校带标签 → 答题 → 看到薪资原文与投稿者留言 → 下一题，循环无报错
- 浏览器 Network 面板里**看不到未作答题目的金额、原文和留言**
- 口径为 `UNKNOWN`、金额解析失败、学校无标签的题目都能正常出题和揭晓，文案不出现空白或「undefined」
- 投稿 → 后台驳回 → 该条不出现在题库；投稿 → 通过 → 能被抽到

- 空题库时前端显示友好提示而非白屏


## 7. 已知风险

| 风险 | 应对 |
|---|---|
| 题量少（仅 30 条） | 先内测不公开；单人连玩 30 题就会重复，投稿量上来前控制曝光 |
| 学校分布极窄（4 所全为京津顶尖校） | 见下节数据缺口；优先补校招渠道的中间层院校 |
| 投稿数据不实（吹高薪资） | 异常值拦截 + 审核 + 样本量足够后按维度中位数做偏离度提示 |
| 题目方差过大导致无法推理 | `tenureText` 在题面展示；口径明示可消掉一部分方差 |
| 口径混杂让玩家困惑 | 题面前置声明要猜的口径；`UNKNOWN` 的题目标注「口径不明」；新投稿走结构化输入 |
| 薪资原文解析失败 | `salaryRaw` 始终可展示，解析仅影响抽样和差异提示，不阻塞出题 |
| 无分数导致缺少留存动力 | 靠投稿者留言提供内容价值 + 「已猜 N 题 / 平均偏差 X%」提供进度感 |
| 单项信息可能引发身份定位投诉 | 管理员可分别隐藏学校或公司 + 举报入口 + 不收身份信息 + 投稿者可主动使用「某大厂」式模糊公司名 |
| 国内部署合规 | 域名 ICP 备案；UGC 需对发布者做真实身份认证（手机号仅用于认证、单独加密存储、不进题库、不展示） |

## 8. 当前数据的缺口

30 条种子数据能跑通流程，但作为题库有三个结构性问题，投稿阶段要针对性补：

**学校只有 4 所，且全是 985/211 顶尖校**（北京交通大学 10 条、北京科技大学 5 条、清华 8 条、北大 7 条）。这是标签体系的直接问题——`一本` `二本` `专科` `QS前100` 这些标签目前一条数据都没有。玩家玩几题就会发现「反正都是名校」，学校维度失去推理价值。投稿渠道要刻意覆盖普通一本、二本、专科和海外院校。

**薪资偏高。** 清华北大样本里有 200w+、120w+、110万/年，整体分布明显高于真实就业市场。玩家会形成「猜高一点总没错」的策略，这会让游戏变得无聊。补数据时优先收 10-25 万区间的样本，那才是大多数人的位置。

**行业分布集中在铁路、土木、计算机三块。** 这是校源单一的连带结果。医疗、教育、快消、法律、金融中后台等基本空缺。

补数据的优先级：先补院校层次多样性，再补薪资中位区间，最后补行业覆盖。前两项直接影响游戏是否好玩，第三项只影响丰富度。

## 9. 评分功能的重启条件

取消评分是暂缓，不是放弃。满足以下条件时回来做：

- 单一口径（建议「税前年薪」）的样本达到 150 条以上
- 投稿表单已稳定运行，口径字段的填错率降到可接受
- 已能对同维度组合算出中位数，用它替代单条样本作为答案

届时 `GameRound.guessAmount` 里积累的历史猜测可以直接回溯计算分数，不会因为现在不做而丢数据——这是保留该字段的理由。

