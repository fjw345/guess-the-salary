{
  "framework": "vite",
  "installCommand": "npm ci",
  "buildCommand": "npm run prisma:generate -w @guess-salary/api && npm run build -w @guess-salary/shared && npm run build -w @guess-salary/web",
  "outputDirectory": "apps/web/dist",
  "rewrites": [{ "source": "/((?!api(?:/|$)).*)", "destination": "/index.html" }]
}
